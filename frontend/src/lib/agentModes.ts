/** Shared agent-mode presets — used by both the "+" menu and the sidebar's "Research mode" nav shortcut. */
export const RESEARCH_MODE = {
  label: 'Research',
  effort: 'max',
  hint:
    'Do thorough, multi-step research: search the web, follow up on ambiguous or conflicting results, ' +
    'and cite where each fact came from before giving a final answer.',
}

export const WEB_SEARCH_MODE = {
  label: 'Web search',
}
